# Hash

## Introduction

Hash is a data-type in Redis. It represents a collection of fields (a field is a pair name=value)

## Questions

1. How to create a key containing a hash in Redis?
2. How to list the properties of a hash in Redis?
3. How to remove one property of a hash in Redis?
4. How to remove all properties of a hash in Redis?
5. Can properties inside a hash have different TTL?
6. How many properties a hash contains?
7. What is the max number of properties a hash can have?
8. Can I list only the property names of a hash?
9. Can I list only the property values of a hash?
10. Can I list only the properties matching a pattern in a hash?
